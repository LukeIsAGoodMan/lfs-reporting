spark:
  app_name: model_reporting
  config:
    spark.sql.shuffle.partitions: "200"
    spark.sql.adaptive.enabled: "true"

output:
  database: reporting
  format: parquet
  mode: overwrite
  partition_cols:
    - score_month
    - model_version

logging:
  level: INFO
